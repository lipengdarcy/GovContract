<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.witsafe.contracts.dao.security.SecUserMapper">


	<!-- 根据用户ID查询该用户所拥有的权限列表 -->
	<select id="getAuthoritiesName" resultType="java.lang.String"
		parameterType="java.lang.Integer">
		select a.name from sec_user u
		left outer join
		sec_role_user ru on u.id=ru.user_id
		left outer join sec_role r on
		ru.role_id=r.id
		left outer join sec_role_authority ra on r.id =
		ra.role_id
		left outer join sec_authority a on ra.authority_id = a.id
		where u.id= #{userId,jdbcType=INTEGER}
	</select>

	<!-- 根据用户ID查询该用户所拥有的角色列表 -->
	<select id="getRolesName" resultType="java.lang.String"
		parameterType="java.lang.Integer">
		select r.name from sec_user u
		left outer join sec_role_user ru on u.id=ru.user_id
		left outer join sec_role r on ru.role_id=r.id
		where u.id= #{userId,jdbcType=INTEGER}
	</select>



</mapper>